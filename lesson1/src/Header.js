const Header = () => {
  const headerStyle = {
    backgroundColor: "#CC998F",
    color: "#3C3231",
  };
  return (
    <header style={headerStyle}>
      <h1>Grocery List</h1>
    </header>
  );
};

export default Header;
